{% if sso_login %}
    {% import '@SyliusAdmin/shared/helper/button.html.twig' as button %}

    {{ button.default({
        text: 'saml_auth.ui.log_by_sso'|trans,
        url: path('glevents_admin_saml_login'),
        icon: 'tabler:lock',
        class: 'btn-secondary w-100 mt-3',
    }) }}
{% endif %}
